# Web Framework
flask==3.0.0
flask-cors==4.0.0

# Database
python-dotenv==1.0.0
supabase>=2.3.0

# PDF/DOCX/PPTX Processing
PyPDF2==3.0.1
pdfplumber>=0.11.0
python-pptx==0.6.21
python-docx==0.8.11

# AI Models (Hugging Face Transformers)
transformers>=4.45.0
torch>=2.1.0
sentence-transformers>=4.0.0
safetensors>=0.4.3
peft>=0.12.0
protobuf>=5.0.0
numpy>=1.24.0

# BERTopic lightweight inference (full bertopic library optional)
# We load topic embeddings + representations directly from saved files.
# bertopic>=0.16.0  # optional - only needed for full BERTopic.load()

# Utilities
requests>=2.32.0
scikit-learn>=1.5.0
httpx>=0.25,<0.28
websockets>=14,<16
